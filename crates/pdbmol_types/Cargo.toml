[package]
name = "pdbmol_types"
version = "0.1.0"
edition = "2021"

[dependencies]
itertools = "0.13.0"
strum = { version = "0.26.3", features = ["derive"] }
thiserror = "1.0.63"

[lints]
workspace = true
