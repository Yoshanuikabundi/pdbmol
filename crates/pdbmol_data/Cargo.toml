[package]
name = "pdbmol_data"
version = "0.1.0"
edition = "2021"

[dependencies]
strum = { version = "0.26.3", features = ["derive"] }

[dev-dependencies]
itertools = "0.13.0"

[lints]
workspace = true
